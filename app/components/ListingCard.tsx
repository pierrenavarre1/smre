import Link from 'next/link'; import Image from 'next/image'; import type { RESOProperty } from '../lib/mock-properties'; import { SourceBadge } from './MLSDisclosure';
export function ListingCard({p}:{p:RESOProperty}){return <Link href={`/listings/${p.ListingId}`} className="listing-card"><div className="card-image"><Image src={p.Media[0].MediaURL} alt={`${p.StreetNumber} ${p.StreetName}, ${p.City}`} fill sizes="(max-width: 700px) 100vw, 33vw"/></div><div className="card-body"><div className="card-top"><strong>{new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(p.ListPrice)}</strong><SourceBadge source={p.MlsSource}/></div><div className="facts">{p.BedroomsTotal} bd · {p.BathroomsTotalInteger} ba {p.LivingArea?`· ${p.LivingArea.toLocaleString()} sq ft`:''}</div><div>{p.StreetNumber} {p.StreetName}, {p.City}, KS {p.PostalCode}</div></div></Link>}
